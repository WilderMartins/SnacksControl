# ===============================================================
# Variáveis de Ambiente do Banco de Dados (para Produção)
# ===============================================================
# O ambiente de desenvolvimento padrão usa SQLite e não requer estas variáveis.
# Preencha as variáveis abaixo apenas para configurar um ambiente de produção com PostgreSQL.
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=exa_snacks

# ===============================================================
# Configurações Gerais da Aplicação
# ===============================================================
# Chave secreta para gerar tokens JWT. Use um valor longo e aleatório em produção.
APP_SECRET=your_jwt_secret

# URL base da aplicação. Usado para gerar links em e-mails (ex: redefinição de senha).
# Em produção, deve ser a URL pública do seu frontend.
APP_URL=http://localhost:3000
